#!/bin/bash

SCRIPT_DIR="$( cd -- "$(dirname $(dirname -- "${BASH_SOURCE[0]}" ))" &> /dev/null && pwd )"; source $SCRIPT_DIR/lib/import.sh

import messages

nmcli radio wifi off
fail "wifi disabled"
sleep 1
nmcli radio wifi on
success "wifi enabled"